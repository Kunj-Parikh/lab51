{
  "name": "contacts-web-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "buildfrontend": "rm -rf dist && cd ../contacts-frontend-mongodb && npm run build && cp -r dist ../contacts-backend-mongodb",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.1",
    "mongoose": "^8.9.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
